setup_repos: []

setup_packages:
  - name:
      - http://dl.fedoraproject.org/pub/epel/epel-release-latest-7.noarch.rpm
      - http://www.elrepo.org/elrepo-release-7.0-3.el7.elrepo.noarch.rpm
    state: present

  - name:
      - kernel-devel
      - git
      - ruby
      - rubygems
      - wget
      - net-tools
      - htop
      - dkms
      - telnet
      - socat
      - "@Development Tools"
    update_cache: yes
    state: latest

setup_libvirt_group: "libvirt"
